package character;

import food.Salt;
import place.Place;

public class Alice extends Human {
    public Alice(String name, double height, double weight, double balance, Salt salt, Place place, boolean seller) {
        super(name, height, weight, balance, salt, place, seller);
    }
}
